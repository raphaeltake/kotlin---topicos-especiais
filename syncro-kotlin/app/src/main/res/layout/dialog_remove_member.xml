<?xml version="1.0" encoding="utf-8"?>
<!-- Referência: 16 - Confirmar exclusão de membro.png. Paleta e componentes dos layouts existentes. -->
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/bg_light"
    android:fillViewport="false"
    android:clipToPadding="false"
    android:overScrollMode="ifContentScrolls">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">
        <TextView
            android:id="@+id/tvTitle"
            style="@style/Screen.Title"
            android:text="@string/screen_confirm_remove_member"
            android:layout_marginBottom="16dp" />

        <TextView
            android:id="@+id/tvMemberName"
            style="@style/Screen.Body"
            android:text="@string/screen_member_name"
            android:gravity="center" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginTop="24dp">
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnCancel"
                style="@style/Screen.Button.Outline"
                android:text="@string/screen_cancel" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnConfirmRemoveMember"
                style="@style/Screen.Button.Danger"
                android:text="@string/screen_action_btnconfirmremovemember"
                android:layout_marginTop="8dp" />
        </LinearLayout>
    </LinearLayout>
</ScrollView>
